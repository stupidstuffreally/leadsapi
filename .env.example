# Local development uses a SQLite file. For hosting online (e.g. on Vercel)
# with multiple people viewing/updating data at once, switch this to a hosted
# database instead (Postgres, or a hosted SQLite service like Turso) — a
# plain file won't reliably persist writes on serverless hosting.
DATABASE_URL="file:./dev.db"
